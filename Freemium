-- SHINYLEAK ALL RIGHTS RESERVED!!

local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/deividcomsono/Obsidian/refs/heads/main/Library.lua"))()

-- WINDOWS
local Window = Library:CreateWindow({
    Title = "ShinyLeak",
    Footer = "v1.0.0",
    ToggleKeybind = Enum.KeyCode.RightControl,
    Center = true,
    AutoShow = true
})

-- TABS
local MainTab = Window:AddTab("Main", "home")
local ESPTab = Window:AddTab("ESP", "eye")

local LeftGroupbox = MainTab:AddLeftGroupbox("Settings")
local RightGroupbox = MainTab:AddRightGroupbox("Information")

-- TAB BOXES
local Tabbox = MainTab:AddLeftTabbox("Settings")
local Tab1 = Tabbox:AddTab("General")
local Tab2 = Tabbox:AddTab("Advanced")

-- AUTO FARM EGG FUNCTION
local function autoFarmEggs()
    local player = game.Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild("Humanoid")
    local egg = workspace:FindFirstChild("Egg")
    
    if egg then
        local targetPosition = egg:GetPivot().Position
        print("Egg position: ", targetPosition)
        
        -- Make the character hover/fly to the egg
        humanoid:MoveTo(targetPosition)
        
        -- Optional: Set the humanoid to flying state for better movement
        if humanoid:GetState() ~= Enum.HumanoidStateType.Flying then
            humanoid:ChangeState(Enum.HumanoidStateType.Flying)
        end
        
        -- Wait until character reaches the destination
        repeat
            task.wait()
            humanoid:MoveTo(targetPosition)
        until (character.HumanoidRootPart.Position - targetPosition).Magnitude < 5
    else
        print("Egg not found in workspace")
    end
end

-- ADD BUTTON TO GENERAL TAB (Correct syntax from the example)
Tab1:AddButton({
    Name = "Auto Farm Eggs",
    Callback = function()
        autoFarmEggs()
    end
})

-- ESP TAB CONTENT
local ESPGroupbox = ESPTab:AddLeftGroupbox("Egg ESP")

-- ESP Function to highlight eggs
local function enableEggESP()
    local egg = workspace:FindFirstChild("Egg")
    if egg then
        -- Create highlight for the egg
        if not egg:FindFirstChild("ESP_Highlight") then
            local highlight = Instance.new("Highlight")
            highlight.Name = "ESP_Highlight"
            highlight.FillColor = Color3.fromRGB(255, 255, 0) -- Yellow fill
            highlight.OutlineColor = Color3.fromRGB(255, 0, 0) -- Red outline
            highlight.FillTransparency = 0.5
            highlight.OutlineTransparency = 0
            highlight.Parent = egg
        end
        print("ESP enabled for egg")
    else
        print("Egg not found in workspace")
    end
end

-- Function to disable ESP
local function disableEggESP()
    local egg = workspace:FindFirstChild("Egg")
    if egg and egg:FindFirstChild("ESP_Highlight") then
        egg:FindFirstChild("ESP_Highlight"):Destroy()
        print("ESP disabled for egg")
    end
end

-- ESP Toggle
local espEnabled = false
ESPGroupbox:AddToggle({
    Name = "Egg ESP",
    Default = false,
    Callback = function(value)
        espEnabled = value
        if value then
            enableEggESP()
        else
            disableEggESP()
        end
    end
})

ESPGroupbox:AddLabel("Shows eggs through walls")

-- Information tab content
RightGroupbox:AddLabel("ShinyLeak v1.0.0")
RightGroupbox:AddLabel("Made with ❤️")
RightGroupbox:AddLabel("Use ESP tab to see eggs")
RightGroupbox:AddLabel("Click Auto Farm in General tab")
