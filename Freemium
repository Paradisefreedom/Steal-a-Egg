-- SHINYLEAK ALL RIGHTS RESERVED!!
-- Advanced "Steal an Egg" Auto-Farm & ESP Architecture

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local CoreGui = game:GetService("CoreGui")

local LocalPlayer = Players.LocalPlayer
local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/deividcomsono/Obsidian/refs/heads/main/Library.lua"))()

-- STATE VARIABLES
local getgenv = getgenv or function() return _G end
getgenv().AutoFarmEnabled = false
getgenv().ESPEnabled = false
getgenv().FlightSpeed = 100

local NoclipConnection = nil
local EggCache = {}
local ESPFolder = Instance.new("Folder")
ESPFolder.Name = "ShinyLeak_EggESP"
ESPFolder.Parent = CoreGui -- Parented to CoreGui to bypass anti-cheat/game deletion

-- WINDOWS & UI SETUP
local Window = Library:CreateWindow({
    Title = "ShinyLeak | Steal An Egg",
    Footer = "v3.0.0",
    ToggleKeybind = Enum.KeyCode.RightControl,
    Center = true,
    AutoShow = true
})

-- TABS
local MainTab = Window:AddTab("Main", "home")
local LeftGroupbox = MainTab:AddLeftGroupbox("Farming Systems")
local RightGroupbox = MainTab:AddRightGroupbox("Movement Settings")

local ESPTab = Window:AddTab("ESP", "eye")
local ESPGroupbox = ESPTab:AddLeftGroupbox("Visuals")

-- CORE SYSTEMS

-- 1. Efficient Egg Caching System
-- Instead of searching the entire workspace every frame, we cache the eggs once every second.
task.spawn(function()
    while task.wait(1) do
        local newCache = {}
        for _, obj in pairs(Workspace:GetDescendants()) do
            if obj.Name:lower():match("egg") then
                -- Handle both loose parts and complex models safely
                if obj:IsA("BasePart") then
                    table.insert(newCache, obj)
                elseif obj:IsA("Model") then
                    -- Find the first physical part in the model if PrimaryPart is missing
                    local physicalPart = obj.PrimaryPart or obj:FindFirstChildWhichIsA("BasePart", true)
                    if physicalPart then
                        table.insert(newCache, physicalPart)
                    end
                end
            end
        end
        EggCache = newCache
    end
end)

-- 2. ESP Engine
local function UpdateESP()
    -- Clear existing highlights
    for _, child in pairs(ESPFolder:GetChildren()) do
        child:Destroy()
    end

    if not getgenv().ESPEnabled then return end

    -- Apply new highlights (Roblox has a render limit of 31 Highlights at once)
    local count = 0
    for _, eggPart in pairs(EggCache) do
        if count >= 30 then break end -- Prevent engine rendering bugs
        
        local highlight = Instance.new("Highlight")
        highlight.Adornee = eggPart.Parent:IsA("Model") and eggPart.Parent or eggPart
        highlight.FillColor = Color3.fromRGB(255, 215, 0) -- Gold color
        highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
        highlight.FillTransparency = 0.5
        highlight.OutlineTransparency = 0.1
        highlight.Parent = ESPFolder
        
        count = count + 1
    end
end

-- Update ESP continuously as eggs are collected/spawned
task.spawn(function()
    while task.wait(0.5) do
        UpdateESP()
    end
end)

-- 3. Dynamic Targeting 
local function GetClosestEgg()
    local character = LocalPlayer.Character
    if not character or not character:FindFirstChild("HumanoidRootPart") then return nil end
    local rootPart = character.HumanoidRootPart

    local closestEgg = nil
    local shortestDistance = math.huge

    for _, targetPart in pairs(EggCache) do
        if targetPart and targetPart.Parent then
            local distance = (rootPart.Position - targetPart.Position).Magnitude
            if distance < shortestDistance then
                shortestDistance = distance
                closestEgg = targetPart
            end
        end
    end

    return closestEgg
end

-- 4. Anti-Collision (Noclip)
local function ToggleNoclip(state)
    if state then
        if not NoclipConnection then
            NoclipConnection = RunService.Stepped:Connect(function()
                local char = LocalPlayer.Character
                if char then
                    for _, part in pairs(char:GetDescendants()) do
                        if part:IsA("BasePart") and part.CanCollide then
                            part.CanCollide = false
                        end
                    end
                end
            end)
        end
    else
        if NoclipConnection then
            NoclipConnection:Disconnect()
            NoclipConnection = nil
        end
    end
end

-- 5. Tween Flight Logic
local function FlyToTarget(targetPart)
    local character = LocalPlayer.Character
    if not character or not character:FindFirstChild("HumanoidRootPart") then return end
    
    local rootPart = character.HumanoidRootPart
    local humanoid = character:FindFirstChild("Humanoid")
    
    if humanoid and humanoid.Health <= 0 then return end

    -- Anchor character during flight to prevent physics glitching/gravity interference
    rootPart.Anchored = true 

    local distance = (rootPart.Position - targetPart.Position).Magnitude
    local timeToReach = distance / getgenv().FlightSpeed

    local tweenInfo = TweenInfo.new(timeToReach, Enum.EasingStyle.Linear)
    local flightTween = TweenService:Create(rootPart, tweenInfo, {CFrame = CFrame.new(targetPart.Position)})
    
    flightTween:Play()
    flightTween.Completed:Wait()
    
    -- Unanchor upon arrival so Touch events register natively
    rootPart.Anchored = false 

    -- Exploit-level touch enforcement
    if targetPart and rootPart and firetouchinterest then
        firetouchinterest(rootPart, targetPart, 0)
        task.wait(0.05)
        firetouchinterest(rootPart, targetPart, 1)
    end
end

-- UI INTEGRATION

LeftGroupbox:AddToggle('AutoFarmToggle', {
    Text = 'Enable Auto Farm',
    Default = false,
    Tooltip = 'Continuously searches for and flies to the closest egg.',
    Callback = function(Value)
        getgenv().AutoFarmEnabled = Value
        ToggleNoclip(Value) 
        
        if Value then
            task.spawn(function()
                while getgenv().AutoFarmEnabled do
                    local targetEgg = GetClosestEgg()
                    if targetEgg then
                        FlyToTarget(targetEgg)
                    else
                        task.wait(0.5) 
                    end
                    task.wait(0.1) 
                end
            end)
        end
    end
})

RightGroupbox:AddSlider('FlightSpeedSlider', {
    Text = 'Flight Speed',
    Default = 100,
    Min = 50,
    Max = 350,
    Rounding = 0,
    Compact = false,
    Callback = function(Value)
        getgenv().FlightSpeed = Value
    end
})

ESPGroupbox:AddToggle('ESPToggle', {
    Text = 'Egg Highlight ESP',
    Default = false,
    Tooltip = 'Highlights up to 30 of the closest eggs on your screen.',
    Callback = function(Value)
        getgenv().ESPEnabled = Value
        UpdateESP()
    end
})
