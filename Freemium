-- SHINYLEAK ALL RIGHTS RESERVED!!
-- Advanced "Steal an Egg" Architecture: Auto Treadmill, Auto Farm & ESP

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local CoreGui = game:GetService("CoreGui")

local LocalPlayer = Players.LocalPlayer
local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/deividcomsono/Obsidian/refs/heads/main/Library.lua"))()

-- STATE VARIABLES
local getgenv = getgenv or function() return _G end
getgenv().AutoTreadmillEnabled = false
getgenv().AutoFarmEnabled = false
getgenv().ESPEnabled = false
getgenv().FlightSpeed = 100

local NoclipConnection = nil
local EggCache = {}
local ESPFolder = Instance.new("Folder")
ESPFolder.Name = "ShinyLeak_EggESP"
ESPFolder.Parent = CoreGui -- Parented to CoreGui to bypass game deletion

-- WINDOWS & UI SETUP
local Window = Library:CreateWindow({
    Title = "ShinyLeak | Steal An Egg",
    Footer = "v4.0.0",
    ToggleKeybind = Enum.KeyCode.RightControl,
    Center = true,
    AutoShow = true
})

-- TABS
local MainTab = Window:AddTab("Main", "home")
local LeftGroupbox = MainTab:AddLeftGroupbox("Farming Systems")
local RightGroupbox = MainTab:AddRightGroupbox("Movement Settings")

local ESPTab = Window:AddTab("ESP", "eye")
local ESPGroupbox = ESPTab:AddLeftGroupbox("Visuals")

-- CORE SYSTEMS

-- 1. Sync Cache Engine (Powers both ESP and Auto Farm)
task.spawn(function()
    while task.wait(1) do
        local newCache = {}
        for _, obj in pairs(Workspace:GetDescendants()) do
            if obj.Name:lower():match("egg") then
                if obj:IsA("BasePart") then
                    table.insert(newCache, obj)
                elseif obj:IsA("Model") then
                    local physicalPart = obj.PrimaryPart or obj:FindFirstChildWhichIsA("BasePart", true)
                    if physicalPart then
                        table.insert(newCache, physicalPart)
                    end
                end
            end
        end
        EggCache = newCache
    end
end)

-- 2. ESP Engine
local function UpdateESP()
    for _, child in pairs(ESPFolder:GetChildren()) do
        child:Destroy()
    end

    if not getgenv().ESPEnabled then return end

    local count = 0
    for _, eggPart in pairs(EggCache) do
        if count >= 30 then break end -- Roblox render limit protection
        
        local highlight = Instance.new("Highlight")
        highlight.Adornee = eggPart.Parent:IsA("Model") and eggPart.Parent or eggPart
        highlight.FillColor = Color3.fromRGB(255, 215, 0) 
        highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
        highlight.FillTransparency = 0.5
        highlight.OutlineTransparency = 0.1
        highlight.Parent = ESPFolder
        
        count = count + 1
    end
end

task.spawn(function()
    while task.wait(0.5) do
        UpdateESP()
    end
end)

-- 3. Auto Treadmill Logic
local function GetTreadmill()
    local character = LocalPlayer.Character
    if not character or not character:FindFirstChild("HumanoidRootPart") then return nil end
    local rootPart = character.HumanoidRootPart

    local closestTreadmill = nil
    local shortestDistance = math.huge

    for _, obj in pairs(Workspace:GetDescendants()) do
        if obj.Name:lower():match("treadmill") then
            local targetPart = obj:IsA("BasePart") and obj or obj:FindFirstChildWhichIsA("BasePart", true)
            if targetPart then
                local distance = (rootPart.Position - targetPart.Position).Magnitude
                if distance < shortestDistance then
                    shortestDistance = distance
                    closestTreadmill = targetPart
                end
            end
        end
    end
    return closestTreadmill
end

-- 4. Dynamic Targeting (Targeting the Highlighted Eggs)
local function GetClosestEgg()
    local character = LocalPlayer.Character
    if not character or not character:FindFirstChild("HumanoidRootPart") then return nil end
    local rootPart = character.HumanoidRootPart

    local closestEgg = nil
    local shortestDistance = math.huge

    -- Only cycles through eggs found in the Cache (the ones highlighted by ESP)
    for _, targetPart in pairs(EggCache) do
        if targetPart and targetPart.Parent then
            local distance = (rootPart.Position - targetPart.Position).Magnitude
            if distance < shortestDistance then
                shortestDistance = distance
                closestEgg = targetPart
            end
        end
    end

    return closestEgg
end

-- 5. Anti-Collision (Noclip)
local function ToggleNoclip(state)
    if state then
        if not NoclipConnection then
            NoclipConnection = RunService.Stepped:Connect(function()
                local char = LocalPlayer.Character
                if char then
                    for _, part in pairs(char:GetDescendants()) do
                        if part:IsA("BasePart") and part.CanCollide then
                            part.CanCollide = false
                        end
                    end
                end
            end)
        end
    else
        if NoclipConnection then
            NoclipConnection:Disconnect()
            NoclipConnection = nil
        end
    end
end

-- 6. Tween Flight Logic
local function FlyToTarget(targetPart)
    local character = LocalPlayer.Character
    if not character or not character:FindFirstChild("HumanoidRootPart") then return end
    
    local rootPart = character.HumanoidRootPart
    local humanoid = character:FindFirstChild("Humanoid")
    
    if humanoid and humanoid.Health <= 0 then return end

    rootPart.Anchored = true 
    local distance = (rootPart.Position - targetPart.Position).Magnitude
    local timeToReach = distance / getgenv().FlightSpeed

    local tweenInfo = TweenInfo.new(timeToReach, Enum.EasingStyle.Linear)
    local flightTween = TweenService:Create(rootPart, tweenInfo, {CFrame = CFrame.new(targetPart.Position)})
    
    flightTween:Play()
    flightTween.Completed:Wait()
    
    rootPart.Anchored = false 

    if targetPart and rootPart and firetouchinterest then
        firetouchinterest(rootPart, targetPart, 0)
        task.wait(0.05)
        firetouchinterest(rootPart, targetPart, 1)
    end
end

-- UI INTEGRATION

LeftGroupbox:AddToggle('AutoTreadmillToggle', {
    Text = 'Auto Treadmill',
    Default = false,
    Tooltip = 'Teleports you to the nearest treadmill and anchors you on it to farm speed.',
    Callback = function(Value)
        getgenv().AutoTreadmillEnabled = Value
        if Value then
            task.spawn(function()
                while getgenv().AutoTreadmillEnabled do
                    local treadmill = GetTreadmill()
                    local char = LocalPlayer.Character
                    if treadmill and char and char:FindFirstChild("HumanoidRootPart") then
                        -- Teleport slightly above the treadmill so you land on it correctly
                        char.HumanoidRootPart.CFrame = treadmill.CFrame + Vector3.new(0, 3, 0)
                        task.wait(0.5)
                    else
                        task.wait(1)
                    end
                end
            end)
        end
    end
})

LeftGroupbox:AddToggle('AutoFarmToggle', {
    Text = 'Auto Farm Eggs',
    Default = false,
    Tooltip = 'Flies directly to the closest egg highlighted by your ESP.',
    Callback = function(Value)
        getgenv().AutoFarmEnabled = Value
        ToggleNoclip(Value) 
        
        if Value then
            task.spawn(function()
                while getgenv().AutoFarmEnabled do
                    local targetEgg = GetClosestEgg()
                    if targetEgg then
                        FlyToTarget(targetEgg)
                    else
                        task.wait(0.5) 
                    end
                    task.wait(0.1) 
                end
            end)
        end
    end
})

RightGroupbox:AddSlider('FlightSpeedSlider', {
    Text = 'Flight Speed',
    Default = 100,
    Min = 50,
    Max = 350,
    Rounding = 0,
    Compact = false,
    Callback = function(Value)
        getgenv().FlightSpeed = Value
    end
})

ESPGroupbox:AddToggle('ESPToggle', {
    Text = 'Egg Highlight ESP',
    Default = false,
    Tooltip = 'Highlights up to 30 of the closest eggs on your screen.',
    Callback = function(Value)
        getgenv().ESPEnabled = Value
        UpdateESP()
    end
})
