-- SHINYLEAK ALL RIGHTS RESERVED!!
-- Advanced "Steal an Egg" Architecture v10.0.0 (Pure Frame-by-Frame Glide Edition)

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local CoreGui = game:GetService("CoreGui")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local LocalPlayer = Players.LocalPlayer
local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/deividcomsono/Obsidian/refs/heads/main/Library.lua"))()

-- ==========================================
-- STATE VARIABLES & CONFIGURATION
-- ==========================================
local getgenv = getgenv or function() return _G end
getgenv().AutoTreadmillEnabled = false
getgenv().AutoFarmEnabled = false
getgenv().AutoUpgradeEnabled = false
getgenv().AutoClaimEnabled = false
getgenv().ESPEnabled = false
getgenv().FlightSpeed = 100

local NoclipConnection = nil
local EggCache = {}
local ESPFolder = Instance.new("Folder")
ESPFolder.Name = "ShinyLeak_EggESP"
ESPFolder.Parent = CoreGui 

-- ==========================================
-- WINDOWS & UI SETUP
-- ==========================================
local Window = Library:CreateWindow({
    Title = "ShinyLeak | Steal An Egg",
    Footer = "v10.0.0 - PURE GLIDE EDITION",
    ToggleKeybind = Enum.KeyCode.RightControl,
    Center = true,
    AutoShow = true
})

local MainTab = Window:AddTab("Main", "home")
local LeftGroupbox = MainTab:AddLeftGroupbox("Farming Systems")
local RightGroupbox = MainTab:AddRightGroupbox("Automation & Upgrades")

local MovementTab = Window:AddTab("Movement", "navigation")
local MovementGroupbox = MovementTab:AddLeftGroupbox("Flight Settings")

local ESPTab = Window:AddTab("Visuals", "eye")
local ESPGroupbox = ESPTab:AddLeftGroupbox("ESP Settings")

-- ==========================================
-- CORE SYSTEMS: FILTERING & CACHING
-- ==========================================
local function IsInPlayerBase(obj)
    local ancestor = obj.Parent
    while ancestor and ancestor ~= game do
        local name = ancestor.Name:lower()
        if name:match("base") or name:match("plot") or name:match("garden") or name:match("tycoon") or name:match("player") then
            return true
        end
        ancestor = ancestor.Parent
    end
    return false
end

local function GetMyTreadmill()
    local character = LocalPlayer.Character
    if not character or not character:FindFirstChild("HumanoidRootPart") then return nil end
    local rootPart = character.HumanoidRootPart

    local closest = nil
    local shortestDistance = math.huge

    for _, obj in pairs(Workspace:GetDescendants()) do
        if obj.Name:lower():match("treadmill") then
            local part = nil
            if obj:IsA("BasePart") then part = obj
            elseif obj:IsA("Model") and obj.PrimaryPart then part = obj.PrimaryPart
            elseif obj:IsA("Model") then part = obj:FindFirstChildWhichIsA("BasePart", true) end

            if part then
                local dist = (rootPart.Position - part.Position).Magnitude
                if dist < shortestDistance then
                    shortestDistance = dist
                    closest = part
                end
            end
        end
    end
    return closest
end

task.spawn(function()
    while task.wait(1) do
        local newCache = {}
        for _, obj in pairs(Workspace:GetDescendants()) do
            if obj.Name:lower():match("egg") then
                if not IsInPlayerBase(obj) then
                    if obj:IsA("BasePart") then
                        table.insert(newCache, obj)
                    elseif obj:IsA("Model") then
                        local physicalPart = obj.PrimaryPart or obj:FindFirstChildWhichIsA("BasePart", true)
                        if physicalPart then
                            table.insert(newCache, physicalPart)
                        end
                    end
                end
            end
        end
        EggCache = newCache
    end
end)

-- ==========================================
-- CORE SYSTEMS: VISUALS (ESP)
-- ==========================================
local function UpdateESP()
    for _, child in pairs(ESPFolder:GetChildren()) do
        child:Destroy()
    end

    if not getgenv().ESPEnabled then return end

    local count = 0
    for _, eggPart in pairs(EggCache) do
        if count >= 30 then break end
        
        local highlight = Instance.new("Highlight")
        highlight.Adornee = eggPart.Parent:IsA("Model") and eggPart.Parent or eggPart
        highlight.FillColor = Color3.fromRGB(255, 215, 0)
        highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
        highlight.FillTransparency = 0.5
        highlight.OutlineTransparency = 0.1
        highlight.Parent = ESPFolder
        
        count = count + 1
    end
end

task.spawn(function()
    while task.wait(0.5) do
        UpdateESP()
    end
end)

-- ==========================================
-- CORE SYSTEMS: NOCLIP & MOVEMENT
-- ==========================================
local function GetClosestEgg()
    local character = LocalPlayer.Character
    if not character or not character:FindFirstChild("HumanoidRootPart") then return nil end
    local rootPart = character.HumanoidRootPart

    local closestEgg = nil
    local shortestDistance = math.huge

    for _, targetPart in pairs(EggCache) do
        if targetPart and targetPart.Parent then
            local distance = (rootPart.Position - targetPart.Position).Magnitude
            if distance < shortestDistance then
                shortestDistance = distance
                closestEgg = targetPart
            end
        end
    end
    return closestEgg
end

local function ToggleNoclip(state)
    if state then
        if not NoclipConnection then
            NoclipConnection = RunService.Stepped:Connect(function()
                local char = LocalPlayer.Character
                if char then
                    for _, part in pairs(char:GetDescendants()) do
                        if part:IsA("BasePart") and part.CanCollide then
                            part.CanCollide = false
                        end
                    end
                end
            end)
        end
    else
        if NoclipConnection then
            NoclipConnection:Disconnect()
            NoclipConnection = nil
        end
    end
end

-- NEW: Pure Frame-by-Frame CFrame Glide. No Anchors. No Tweens. No BodyVelocity.
local function FlyToTarget(targetPart)
    local character = LocalPlayer.Character
    if not character or not character:FindFirstChild("HumanoidRootPart") then return end
    
    local rootPart = character.HumanoidRootPart

    -- Glides in a straight line until we are right on top of the egg
    while getgenv().AutoFarmEnabled and targetPart and targetPart.Parent do
        local distance = (targetPart.Position - rootPart.Position).Magnitude
        
        -- If we are close enough, grab the egg and break the loop
        if distance < 3 then
            if firetouchinterest then
                firetouchinterest(rootPart, targetPart, 0)
                task.wait(0.01)
                firetouchinterest(rootPart, targetPart, 1)
            end
            break
        end
        
        -- Calculate direction and how far to move this specific frame
        local deltaTime = RunService.Heartbeat:Wait()
        local direction = (targetPart.Position - rootPart.Position).Unit
        local step = direction * (getgenv().FlightSpeed * deltaTime)
        
        -- Smoothly slide the player
        rootPart.CFrame = rootPart.CFrame + step
        
        -- Nullify physical momentum so gravity doesn't drop you and anti-cheat doesn't flag velocity
        rootPart.AssemblyLinearVelocity = Vector3.zero
        rootPart.AssemblyAngularVelocity = Vector3.zero
    end
end

-- ==========================================
-- CORE SYSTEMS: AUTOMATION (UPGRADES & CLAIMING)
-- ==========================================
local function ExecuteUpgrades()
    for _, remote in pairs(ReplicatedStorage:GetDescendants()) do
        if remote:IsA("RemoteEvent") then
            local name = remote.Name:lower()
            if name:match("upgrade") or name:match("buy") or name:match("purchase") then
                pcall(function() remote:FireServer() end)
                pcall(function() remote:FireServer("Treadmill") end)
            end
        end
    end
end

local function ExecuteClaimIndex()
    for _, remote in pairs(ReplicatedStorage:GetDescendants()) do
        if remote:IsA("RemoteEvent") then
            local name = remote.Name:lower()
            if name:match("claim") or name:match("index") or name:match("reward") or name:match("quest") then
                pcall(function() remote:FireServer() end)
                pcall(function() remote:FireServer("ClaimAll") end)
                pcall(function() remote:FireServer("Index") end)
            end
        elseif remote:IsA("RemoteFunction") then
            local name = remote.Name:lower()
            if name:match("claim") or name:match("index") then
                task.spawn(function()
                    pcall(function() remote:InvokeServer() end)
                end)
            end
        end
    end
end

-- ==========================================
-- UI INTEGRATION & TOGGLES
-- ==========================================

LeftGroupbox:AddToggle('AutoFarmToggle', {
    Text = 'Auto Farm Public Eggs',
    Default = false,
    Tooltip = 'Smooth frame-by-frame glide to bypass anti-cheat.',
    Callback = function(Value)
        getgenv().AutoFarmEnabled = Value
        ToggleNoclip(Value) 
        if Value then
            task.spawn(function()
                while getgenv().AutoFarmEnabled do
                    local targetEgg = GetClosestEgg()
                    if targetEgg then
                        FlyToTarget(targetEgg)
                    else
                        task.wait(0.1) 
                    end
                end
            end)
        end
    end
})

LeftGroupbox:AddToggle('AutoTreadmillToggle', {
    Text = 'Auto Treadmill',
    Default = false,
    Tooltip = 'Teleports safely to YOUR base treadmill.',
    Callback = function(Value)
        getgenv().AutoTreadmillEnabled = Value
        if Value then
            task.spawn(function()
                while getgenv().AutoTreadmillEnabled do
                    if not getgenv().AutoFarmEnabled then
                        local character = LocalPlayer.Character
                        local root = character and character:FindFirstChild("HumanoidRootPart")
                        local myTreadmill = GetMyTreadmill()
                        
                        if root and myTreadmill then
                            root.CFrame = myTreadmill.CFrame * CFrame.new(0, 2.5, 0)
                        end
                    end
                    task.wait(0.5) 
                end
            end)
        end
    end
})

RightGroupbox:AddToggle('AutoUpgradeToggle', {
    Text = 'Auto Upgrade Treadmill',
    Default = false,
    Tooltip = 'Automatically triggers treadmill upgrades.',
    Callback = function(Value)
        getgenv().AutoUpgradeEnabled = Value
        if Value then
            task.spawn(function()
                while getgenv().AutoUpgradeEnabled do
                    ExecuteUpgrades()
                    task.wait(2)
                end
            end)
        end
    end
})

RightGroupbox:AddToggle('AutoClaimToggle', {
    Text = 'Auto Claim Index/Rewards',
    Default = false,
    Tooltip = 'Claims all index rewards continuously.',
    Callback = function(Value)
        getgenv().AutoClaimEnabled = Value
        if Value then
            task.spawn(function()
                while getgenv().AutoClaimEnabled do
                    ExecuteClaimIndex()
                    task.wait(5)
                end
            end)
        end
    end
})

MovementGroupbox:AddSlider('FlightSpeedSlider', {
    Text = 'Flight Speed',
    Default = 100,
    Min = 25,
    Max = 300,
    Rounding = 0,
    Compact = false,
    Callback = function(Value)
        getgenv().FlightSpeed = Value
    end
})

ESPGroupbox:AddToggle('ESPToggle', {
    Text = 'Egg Highlight ESP',
    Default = false,
    Tooltip = 'Highlights public map eggs in gold.',
    Callback = function(Value)
        getgenv().ESPEnabled = Value
        UpdateESP()
    end
})
